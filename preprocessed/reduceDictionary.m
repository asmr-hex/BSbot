function [histograms, labels, info] = reduceDictionary(histograms, labels, info)
%Using tf-idf method, reduce dictionary

%Get number of documents
N = length(labels);

%Initialize Document Frequency for each entry in dictionary
DF = [];

end