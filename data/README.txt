#READ ME#

The data contained within this directory is the fully processed data— it has
been converted from text documents to continuous features.

Each data file (Train.txt and Test.txt) are formatted as N x D tab delimited matrices. Where N is the sample size and D is the feature dimension. Labels.txt within the Train directory contains the labels corresponding to each row of the matrix contained in Train.txt